<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">All questions</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= new_question_path %>">Create a new question</a>
        </li>
      </ul>
      <!-- If the user is logged in, show a signout button -->
      <% if account_signed_in? %>
        <form action="<%= destroy_account_session_path %>" method="POST">
          <input type="hidden" name="_method" value="DELETE">
          <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
          <input type="submit" value="Sign out" class="btn btn-outline-danger">
        </form>
      <% else %>
        <a class="btn btn-success" href="<%= new_account_session_path %>">Sign in</a>
      <% end %>
    </div>
  </div>
</nav>
